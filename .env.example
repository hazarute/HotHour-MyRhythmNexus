# HotHour environment example
# Copy this to .env and fill values for your environment.

# ============================================================================
# APP CONFIGURATION
# ============================================================================
APP_ENV=development
DEBUG=true
SECRET_KEY=change-me-to-a-secure-random-string
TZ=UTC
LOG_LEVEL=DEBUG

# ============================================================================
# DATABASE (PostgreSQL + Prisma)
# ============================================================================
# Prisma Client Python ile kullanılır
DATABASE_URL=postgresql://hothour_user:password@localhost:5433/hothour_db?connection_limit=5

# ============================================================================
# REDIS (Socket.IO message queue ve caching)
# ============================================================================
REDIS_URL=redis://localhost:6379/0
SOCKETIO_MESSAGE_QUEUE=redis://localhost:6379/1

# ============================================================================
# WEB / CORS / SECURITY
# ============================================================================
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ORIGINS=http://localhost:3000
BACKEND_CORS_ORIGINS=["http://127.0.0.1:5173","http://localhost:5173","http://localhost:3000","http://127.0.0.1:8000","http://localhost:8000"]

# ============================================================================
# FRONTEND URL (Email doğrulama linklerinde kullanılır)
# ============================================================================
# Development: http://localhost:3000
# Production: https://your-domain.com
FRONTEND_URL=http://localhost:3000

# ============================================================================
# EMAIL CONFIGURATION (Gmail SMTP)
# ============================================================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_user=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Email ayarları
EMAILS_FROM_EMAIL=noreply@your-domain.com
EMAILS_FROM_NAME=HotHour

# Not: SMTP_HOST, SMTP_PORT, EMAILS_FROM_EMAIL ayarlanırsa
# EMAILS_ENABLED otomatik olarak True olur
# Gmail için 2-Factor Authentication aktif ederek App Password oluşturmanız gerekir

# ============================================================================
# MONITORING & ERROR TRACKING (Optional)
# ============================================================================
SENTRY_DSN=

# ============================================================================
# PAYMENT INTEGRATION (Optional)
# ============================================================================
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
PAYMENTS_ENABLED=false

# ============================================================================
# PRODUCTION DEPLOYMENT ÖRNEK
# ============================================================================
# Aşağıda Production ortamına geçerken ayarlanması gereken değerler örneği verilmiştir:
#
# APP_ENV=production
# DEBUG=false
# SECRET_KEY=<strong-random-key-here>
# FRONTEND_URL=https://hothour.com
#
# Database (Production PostgreSQL)
# DATABASE_URL=postgresql://prod_user:strong-password@prod-host:5432/hothour_prod
#
# Email (SendGrid, AWS SES, veya kurum içi SMTP)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_user=apikey
# SMTP_PASSWORD=<your-sendgrid-api-key>
# EMAILS_FROM_EMAIL=noreply@hothour.com
#
# Redis (Production Redis)
# REDIS_URL=redis://:password@prod-redis-host:6379/0
